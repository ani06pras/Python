def Armstrong(a):
    b = str(a)
    c=0
    for i in b:
        c = c + (int(i)**3)
        print(c)
    if c==a:
        return "Armstrong"
    else:
        return "Your arm is not strong"
num1 = int(input("Enter the number: "))
print(Armstrong(num1))
